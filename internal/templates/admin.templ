package templates

templ Admin() {
    @Title("Admin")
    <article>
        <h2>Admin Page</h2>
        <p>This is the super secret admin page. Only authorized users should be here.</p>
        <button onclick="alert('Secret button clicked!')">Secret Button</button>
    </article>
}